<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Compose apps (no action bar) -->
    <style name="Theme.FinRadar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Compose yüklenene kadar siyah ekran yerine uygulama arka planı göster -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Android 12+ sistem splash screen arka planı -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
